
No, the first method, rolling_buffer1 mutates the array that is passed to it (buffer) as an argument, while the second method, 
rolling_buffer2 does not alter the array (input_array) that is passed to it.
